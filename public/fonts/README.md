# Fuentes

El mockup usa las tipografías mediante paquetes Fontsource:

- `@fontsource/bebas-neue`
- `@fontsource/outfit`

Esta carpeta queda disponible solo si el proyecto decide reemplazar Fontsource por archivos locales propios.
